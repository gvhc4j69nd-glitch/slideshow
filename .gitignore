node_modules/
.DS_Store
*.log
.env
.env.*

# The photo library itself stays out of git — only the folder placeholder is tracked.
photos/*
!photos/.gitkeep

# Accounts and the signing key never belong in the repo.
data/
