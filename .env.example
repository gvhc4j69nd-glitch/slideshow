# Copy to .env for local development. Railway supplies these itself.
DATABASE_URL=postgres://localhost/vinboo

# Optional
# SIGNUP_CODE=some-shared-code       # require a shared code to create an account
# SITE_HOST=vinboo.com               # the address shown to a presenter to read out
# SESSION_SECRET=                    # otherwise generated once and kept in the database
